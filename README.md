# validate-ssd1305

## get submodules recursively
```bash
git submodule update --init --recursive
```

## init
```bash
./init.sh
```

## compile (configure & make)
```bash
./compile.sh
```

## connect Pico

## flash
```bash
./flash.sh
```